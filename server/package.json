{
  "scripts": {
    "start": "json-server db.json -m ./node_modules/json-server-auth -p 2000 -r routes.json"
  },
  "devDependencies": {
    "json-server": "^0.16.3",
    "json-server-auth": "^2.0.2"
  }
}
